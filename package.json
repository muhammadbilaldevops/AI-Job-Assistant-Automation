{
  "name": "ai-job-assistant-automation",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A local-first job search workspace with an exact resume preset and optional Make and Ollama adapters.",
  "scripts": { "start": "node --env-file-if-exists=.env server.mjs", "test": "node --test tests/*.test.mjs", "check": "node scripts/check.mjs" },
  "engines": { "node": ">=22" }
}
