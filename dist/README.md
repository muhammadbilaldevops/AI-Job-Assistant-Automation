# Distribution assets

`dist/prompts/master-resume.txt` is the preserved source prompt used as the contract for resume generation. Its SHA-256 checksum is recorded in `docs/master-prompt.sha256`; update both only when the product owner intentionally changes the prompt.
