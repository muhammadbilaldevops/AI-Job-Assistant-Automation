# Server-only settings. Never commit a real webhook URL or model credential.
MAKE_AI_WEBHOOK_URL=
# Optional legacy intake adapter, unused by the new cloud workspace.
MAKE_WEBHOOK_URL=
OLLAMA_MODEL=
# Run using: node --env-file=.env server.mjs
