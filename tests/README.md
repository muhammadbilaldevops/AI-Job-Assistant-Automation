# Tests

Tests cover parsing fallbacks, matching, prompt rules, document validation, persistence and key user flows. Run `npm test` after changes, then `npm run check` and `npm run build` to catch type and production-bundle regressions.
